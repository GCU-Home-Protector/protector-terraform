# Terraform provider/plugins/modules
.terraform/

# Terraform state files
terraform.tfstate
terraform.tfstate.*

.terraform.lock.hcl

# Sensitive variable files (if you use them)
*.tfvars
*.tfvars.json

# Crash log files
crash.log
crash.*.log

# Override and plan files (optional)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfplan

# CLI configuration files
.terraformrc
terraform.rc

# (선택) var.tf 파일도 제외하려면 아래 추가
var.tf
